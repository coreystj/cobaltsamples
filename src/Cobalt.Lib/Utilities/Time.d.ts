/**
 * Utility class for time-related operations.
 * 
 * Example Usage:
 * ```ts
 * Debug.Log(Time.deltaTime); // Output: Time { deltaTime: 0 }
 * ```
 */
export class Time {
    /**
     * Represents the time difference between frames.
     */
    public static deltaTime: number;
}
