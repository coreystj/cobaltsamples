
import { CobaltScript } from "../Cobalt.Lib/Behaviours/CobaltScript";

export class PathBehaviour extends CobaltScript {
    constructor(id: string) {
        super(id);
    }

    // Initialization, executed once
    public Start() {
    }

    public Update() {
    }
}
